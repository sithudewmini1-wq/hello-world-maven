A basic Maven-managed Java project ("hello-world") demonstrating core Maven concepts — standard directory structure, dependency management via pom.xml, and the build lifecycle (compile, test, package). The project includes a simple App.java class and a corresponding JUnit test in AppTest.java, verifying that the application builds, compiles, and runs correctly using Maven commands.
**Module:** Software Engineering
**Name:** N.K.S. Dewmini
**Registration Number:** 244044J